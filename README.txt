To compile this project use compile.sh script.

This project is using C++ version 20, and compiler G++ version 10

Standalone test binary is located in bin/ folder (after compilation)

To run test binary, enter two command line arguments
./test <number of variables> <number of repeat cycles>